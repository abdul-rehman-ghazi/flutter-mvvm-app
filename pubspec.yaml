name: demo_app
description: A demo application

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  dio: ^5.3.3
  flutter:
    sdk: flutter
  get_it: ^7.6.4
  logger: ^2.0.2+1
  shared_preferences: ^2.2.2
  stacked: ^3.4.1+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3

flutter:
  uses-material-design: true

assets:
  - assets/
  - assets/icons/